import SubscribePage from "@/components/subscribe/page";
import Layout from "@/container/layout";

const userId = () => {
  //* this page will generate
  return <SubscribePage isAdmin={false} />;
};

export default userId;
